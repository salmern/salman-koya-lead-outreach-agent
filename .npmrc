# The Claude Agent SDK ships its native CLI binary as optional platform
# packages. Ensure optional dependencies are always installed so the
# binary is present on Vercel's linux-x64 runtime.
include=optional
